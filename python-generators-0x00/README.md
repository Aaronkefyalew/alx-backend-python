connect_db() → connects to MySQL (without db).

create_database() → creates ALX_prodev if missing.

connect_to_prodev() → reconnects to that db.

create_table() → ensures user_data exists.

insert_data() → reads user_data.csv and inserts rows.
